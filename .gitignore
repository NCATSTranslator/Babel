# The private input files directory is for files that should not be included in
# Git or Docker, such as UMLS files.
input_files/private 
__pycache__/
babel_downloads/
babel_outputs/
.snakemake/
.env
.idea
htmlcov/

# Source-impact detail file: generated locally for digging/sharing, but not committed —
# it grows roughly linearly with the source and is useless to commit for large additions.
# The smaller CSV/TSV detail files in the same directory are committed.
docs/sources/*/impact-report/modified-cliques.json
data/

# Build metadata regenerated by `uv sync` / setuptools; never committed.
*.egg-info/

# Coverage data written by `pytest --cov`.
.coverage
.coverage.*
htmlcov/
