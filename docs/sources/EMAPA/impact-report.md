# Source impact report: EMAPA

This report has not been generated yet.

It is produced by the source-impact report tool once EMAPA's intermediate `ids` and
`concords` files have been built:

```bash
uv run source-impact-report --source EMAPA
```

See "Adding a new data source" in `CLAUDE.md` and `docs/AddingNewSources.md` for the
full workflow. Once generated, this file will record the identifiers, biolink types,
cross-references, and clique changes EMAPA introduces.
